# Creatable Select - UX Patterns

> **Visual guide for inline vs modal creation patterns**

---

## 🎨 Pattern 1: Inline Creation (Type-to-Create)

### Visual Flow

```
Step 1: Click to open dropdown
┌────────────────────────────────────┐
│ Select tags...              [▼]    │ ← Closed state
└────────────────────────────────────┘


Step 2: Dropdown opens, shows existing options
┌────────────────────────────────────┐
│ [Search tags...            ] [×]   │ ← Search input
├────────────────────────────────────┤
│ ✓ React                            │ ← Selected option
│   TypeScript                       │
│   Next.js                          │
│   Tailwind CSS                     │
└────────────────────────────────────┘


Step 3: User types "Machine Learning"
┌────────────────────────────────────┐
│ [Machine Learning          ] [×]   │ ← User typing
├────────────────────────────────────┤
│ No results found                   │ ← Empty state
└────────────────────────────────────┘


Step 4: "Create" button appears
┌────────────────────────────────────┐
│ [Machine Learning          ] [×]   │
├────────────────────────────────────┤
│ ➕ Create "Machine Learning"       │ ← CREATE BUTTON
└────────────────────────────────────┘


Step 5: User clicks "Create" → Success!
┌────────────────────────────────────┐
│ [Search tags...            ] [×]   │
├────────────────────────────────────┤
│ ✓ React                            │
│ ✓ Machine Learning (new)           │ ← New option created & selected
│   TypeScript                       │
│   Next.js                          │
└────────────────────────────────────┘
```

### Code Example

```typescript
<CommandEmpty>
  {search && !searchValueExists ? (
    <button
      onClick={handleCreateOption}
      className="flex w-full items-center gap-2 px-2 py-1.5"
    >
      <Plus className="h-4 w-4" />
      <span>Create "{search}"</span>
    </button>
  ) : (
    <div>No results found</div>
  )}
</CommandEmpty>
```

---

## 🎨 Pattern 2: Modal Creation (Form-Based)

### Visual Flow

```
Step 1: Dropdown with "Add New" option
┌────────────────────────────────────┐
│ Select category...          [▼]    │
└────────────────────────────────────┘
        ↓ Click
┌────────────────────────────────────┐
│ [Search...                ] [×]    │
├────────────────────────────────────┤
│   Electronics                      │
│   Books                            │
│   Toys                             │
├────────────────────────────────────┤
│ ➕ Create New Category             │ ← Click this
└────────────────────────────────────┘


Step 2: Modal opens
┌──────────────────────────────────────────┐
│  Create New Category               [×]   │ ← Modal
├──────────────────────────────────────────┤
│                                          │
│  Name *                                  │
│  [_________________________________]     │
│                                          │
│  Description                             │
│  [_________________________________]     │
│  [_________________________________]     │
│                                          │
│  Parent Category                         │
│  [Select parent...            ▼]         │
│                                          │
│  Icon                                    │
│  [🎯] [📦] [🎨] [💼] [⚙️]             │
│                                          │
│                    [Cancel]  [Create]    │
└──────────────────────────────────────────┘


Step 3: Fill form and submit
┌──────────────────────────────────────────┐
│  Create New Category               [×]   │
├──────────────────────────────────────────┤
│                                          │
│  Name *                                  │
│  [Smart Home Devices_______________]     │ ← Filled
│                                          │
│  Description                             │
│  [Connected devices for home       ]     │
│  [automation and monitoring________]     │
│                                          │
│  Parent Category                         │
│  [Electronics                 ▼]         │ ← Selected
│                                          │
│  Icon                                    │
│  [🎯] [📦] [🎨] [💼] [⚙️]             │ ← Selected 🎯
│                                          │
│                    [Cancel]  [Create]    │
└──────────────────────────────────────────┘
        ↓ Click "Create"
✅ Success toast: "Category created!"
        ↓
Dropdown updates with new option
```

### Code Example

```typescript
<Dialog open={showModal} onOpenChange={setShowModal}>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Create New Category</DialogTitle>
    </DialogHeader>
    <form onSubmit={handleSubmit}>
      <div className="space-y-4">
        <div>
          <Label>Name *</Label>
          <Input {...register("name")} />
        </div>
        <div>
          <Label>Description</Label>
          <Textarea {...register("description")} />
        </div>
        <div>
          <Label>Parent Category</Label>
          <Select {...register("parentId")}>{/* options */}</Select>
        </div>
      </div>
      <DialogFooter>
        <Button type="button" variant="ghost" onClick={closeModal}>
          Cancel
        </Button>
        <Button type="submit">Create</Button>
      </DialogFooter>
    </form>
  </DialogContent>
</Dialog>
```

---

## 🎨 Pattern 3: Hybrid (Inline + Modal)

### Visual Flow

```
Dropdown shows both options
┌────────────────────────────────────┐
│ [Search tags...            ] [×]   │
├────────────────────────────────────┤
│   React                            │
│   TypeScript                       │
│   Next.js                          │
├────────────────────────────────────┤ ← Divider
│ ➕ Quick Create                    │ ← Type-to-create
│ ⚙️  Advanced Create                │ ← Opens modal
└────────────────────────────────────┘
```

**User types → Quick Create appears:**

```
┌────────────────────────────────────┐
│ [Machine Learning          ] [×]   │
├────────────────────────────────────┤
│ ➕ Quick add "Machine Learning"    │ ← Inline creation
├────────────────────────────────────┤
│ ⚙️  Advanced Create                │ ← Modal for complex
└────────────────────────────────────┘
```

---

## 📊 Interactive State Diagram

```
                    ┌─────────────────┐
                    │   User Opens    │
                    │    Dropdown     │
                    └────────┬────────┘
                             │
            ┌────────────────┴────────────────┐
            │                                 │
     ┌──────▼──────┐                  ┌──────▼──────┐
     │ Selects     │                  │ Starts      │
     │ Existing    │                  │ Typing      │
     └──────┬──────┘                  └──────┬──────┘
            │                                 │
     ┌──────▼──────┐              ┌──────────┴──────────┐
     │   Option    │              │                     │
     │  Selected   │      ┌───────▼────────┐  ┌────────▼────────┐
     └─────────────┘      │ Match Found?   │  │  Match Found?   │
                          │     YES        │  │      NO         │
                          └───────┬────────┘  └────────┬────────┘
                                  │                    │
                          ┌───────▼────────┐  ┌────────▼────────┐
                          │  Filter & Show │  │ Show "Create"   │
                          │    Options     │  │     Button      │
                          └────────────────┘  └────────┬────────┘
                                                       │
                                               ┌───────▼────────┐
                                               │ User Clicks    │
                                               │    Create      │
                                               └───────┬────────┘
                                                       │
                                               ┌───────▼────────┐
                                               │   Validate     │
                                               └───────┬────────┘
                                                       │
                                          ┌────────────┴────────────┐
                                          │                         │
                                   ┌──────▼──────┐          ┌──────▼──────┐
                                   │   Valid     │          │   Invalid   │
                                   └──────┬──────┘          └──────┬──────┘
                                          │                         │
                                   ┌──────▼──────┐          ┌──────▼──────┐
                                   │   Create    │          │ Show Error  │
                                   │   & Select  │          │   Toast     │
                                   └──────┬──────┘          └─────────────┘
                                          │
                                   ┌──────▼──────┐
                                   │  Add to     │
                                   │  Options    │
                                   └──────┬──────┘
                                          │
                                   ┌──────▼──────┐
                                   │ Show Badge  │
                                   │   "(new)"   │
                                   └─────────────┘
```

---

## 🎯 Real-World Examples

### Example 1: Gmail-style Email Recipients

```
TO: recipient1@example.com, recipient2@example.com, |
     ↑                       ↑
  (badges/chips)         (cursor typing)
```

**User types:** `newperson@example.com` → Press Enter → Created!

---

### Example 2: Trello-style Labels

```
Labels: [React] [Bug] [Priority: High] [+]
         ↑      ↑      ↑                  ↑
    (colored badges)              (add new button)
```

**Click [+]:**

- Inline: Type name → Press Enter
- Modal: Click [+] → Modal with name + color picker

---

### Example 3: Slack-style Channels

```
#general
#random
#engineering
────────────
+ Create channel  ← Opens modal (needs name, description, privacy)
```

---

## 🔗 Implementation Files

- **Inline Creation:** `CreatableSelect.tsx` (current implementation)
- **Modal Creation:** `CreatableSelectWithModal.tsx` (example above)
- **Hybrid:** `CreatableSelectHybrid.tsx` (to be implemented)

---

## ✅ Summary

| Pattern    | Clicks | Speed       | Best For                           |
| ---------- | ------ | ----------- | ---------------------------------- |
| **Inline** | 2      | ⚡⚡⚡ Fast | Tags, emails, quick labels         |
| **Modal**  | 3-4    | 🐢 Slower   | Complex forms with multiple fields |
| **Hybrid** | 2-4    | ⚡⚡ Medium | Flexibility - let users choose     |

---

**Current Implementation:** Inline (Type-to-Create) ✅  
**Recommended For:** Tags, labels, simple text values  
**Modal Approach:** Use when creating requires 3+ fields
